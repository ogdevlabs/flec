# Core CV and perception
opencv-python>=4.8.0
ultralytics>=8.0.0
# Pinned <0.10.30: newer mediapipe dropped the legacy mp.solutions API used by finger_tracker
mediapipe>=0.10.0,<0.10.30

# Speech: wake word and STT
openwakeword>=0.6.0
openai-whisper

# TTS (Coqui) — needs the espeak-ng system binary for phonemisation
TTS>=0.22.0

# Audio I/O — mic capture (voice commands) and TTS playback
sounddevice>=0.4.6

# OCR and image understanding
easyocr>=1.7.0

# Transformers (BLIP-2)
transformers>=4.35.0
torch>=2.1.0

# Testing
pytest>=7.4.0
hypothesis>=6.90.0
